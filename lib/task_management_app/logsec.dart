import 'package:flutter/material.dart';

class LogSecPage extends StatelessWidget {
  const LogSecPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Login & Security'),
      ),
      body: Center(
        child: Text(
          'Yet to Happen',
        ),
      ),
    );
    ;
  }
}
